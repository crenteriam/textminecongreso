



# Extract the number of words around the word identified.
#stringr::str_extract(text, "([^\\s]+\\s){3}Verulam(\\s[^\\s]+){3}")
# Error in open.connection(x, "rb") : Timeout was reached 

# Graph 1: Frequencies by week.
# Collapse by day, count, collapse by month, count.


